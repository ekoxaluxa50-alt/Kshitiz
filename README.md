# Pursuit of Jade

An interactive single-page adventure app about tracking a mythical jade relic through lantern markets, monsoon ruins, and a hidden observatory.

## Run locally

Open `index.html` in a browser, or serve the folder:

```bash
npm start
```

Then visit `http://localhost:4173`.

## Verify

```bash
npm test
```
